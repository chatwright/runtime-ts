/**
 * `deterministic` — a data-driven, serialisable deterministic assertion
 * evaluator over a chat's journal, preserving the semantics of the Go
 * runtime's fluent `cw` assertion DSL.
 */

export * from "./assertions.js";
